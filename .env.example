# Environment variables for Vast.ai development environment

# Jupyter Configuration
JUPYTER_ENABLE_LAB=yes
JUPYTER_TOKEN=your-secure-token-here

# GPU Configuration
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility

# User Configuration
GRANT_SUDO=yes

# Cloudflare Tunnel (optional)
TUNNEL_ID=your-tunnel-id
TUNNEL_NAME=vastai-dev

# SSH Public Keys (one per line, multiple keys supported)
# Format: Each line should contain the complete public key (key-type key-data comment)
# Example:
# SSH_PUBLIC_KEYS="ssh-ed25519 AAAAC3Nza... user1@localhost
# ssh-rsa AAAAB3Nza... user2@example.com
# ssh-ed25519 AAAAC3Nza... user3@host"
SSH_PUBLIC_KEYS=""

# Custom settings
WORKSPACE_DIR=/workspace
MODELS_DIR=/models
DATASETS_DIR=/datasets
